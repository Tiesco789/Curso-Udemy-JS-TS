/**
 * ARRAYS:
 * Um Array pode ter vários tipos de dados como booleans, strings, numbers, e etc;
 * É recomendado que os arrays possuam apenas um tipo de dados;
 */

const alunos = ['Luiz', 'Maria', 'João'];

console.log(typeof alunos);
console.log(alunos instanceof Array);


console.log(alunos);

// alunos.push('Luiza');
// alunos.push('Eduardo');
// console.log(alunos.slice(0, -1));

// delete alunos[1];
// const alunoRemovido = alunos.pop();
// const alunoAlterado = alunos.shift();

// console.log(alunoAlterado);
// console.log(alunoRemovido);

// alunos.unshift('Jessica'); // Adiciona um item no inicio do array
// alunos.unshift('Mateus'); // Adiciona um item no inicio do array

// alunos.push('Luiza'); Adiciona um item no fim do array
// alunos.push('Fabio'); Adiciona um item no fim do array

// alunos[alunos.length] = 'Luiza'; // Adiciona um item no array
// alunos[alunos.length] = 'Fabio'; // Adiciona um item no array
// alunos[alunos.length] = 'Marcelo'; // Adiciona um item no array

// alunos[0] = 'Eduardo'; // Alterei o primeiro indice do array
// alunos[3] = 'Luiza'; // Adiciono um item ao final do array

// console.log(nomes); // Retorna o array inteiro
// console.log(nomes[0]); // Retorna o primeiro item do array
// console.log(nomes[2]); // Retorna o terceiro item do array
